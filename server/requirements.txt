# Pinned to the versions the test suite runs against (audit M11: unpinned
# deps made Cloud Run builds non-reproducible — staging and prod deployed
# days apart could silently run different fastapi/uvicorn/psycopg versions).
# To upgrade: bump the pin, run the suite, deploy to staging first.
# pydantic is technically transitive (via fastapi) but imported directly by
# main.py, so it's pinned here too.
fastapi==0.136.3
google-genai==2.7.0
httpx==0.28.1
posthog==6.9.3
psycopg[binary]==3.3.4
psycopg_pool==3.3.1
pydantic==2.13.4
pypdf==6.13.1
python-multipart==0.0.32
redis==8.0.0
uvicorn[standard]==0.49.0
