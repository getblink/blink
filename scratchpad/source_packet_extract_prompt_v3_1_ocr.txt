You are preparing cached source context for a later copy-paste action.

Analyze SOURCE_IMAGE only.

Output plain text only. Do not output JSON. Do not use markdown code fences.

Use exactly these sections in exactly this order:

SOURCE_KIND: <terminal_log|conductor_chat|google_doc|note_with_marketplace_listing|marketplace_listing|web_form|unknown>
SCENE: <1-2 concise sentences naming what the source is and what kinds of reusable content it contains>
EXACT_TEXT:
- <exact visible short span>
- <exact visible short span>
TEXT_BLOCKS:
- <exact visible passage with its header attached; see rules below>
- <exact visible passage with its header attached; see rules below>
LAYOUT_HINTS:
- <very short note about structure, ordering, or adjacency that helps a later step pick the right block>
LIMITS:
- <cropped, hidden, illegible, or ambiguous details that matter later; write "- none visible" if none>
COMPLETENESS: <sufficient|needs_source_image>

Primary goal: preserve every piece of visible textual content that a later paste decision may need, verbatim, with its original structural context intact. The later model will only see this text packet. If you drop a paragraph, reword a sentence, or sever a header from its body here, it cannot be recovered later.

Prioritization, in order:
1. Never drop, summarize, shorten, merge, or paraphrase a visible paragraph, message, form answer, log entry, listing row, or document section that could be the thing the user wants to paste. Err on the side of keeping it.
2. Preserve the binding between a visible header/label/question and the text that follows it. Keep the two together in the same TEXT_BLOCK with the header on its own first line and the body on following lines.
3. Preserve exact wording, capitalization, punctuation, inline spacing, line breaks, and visible ordering. Do not rewrite, clean up grammar, normalize punctuation, translate, re-wrap, or truncate with "...".
4. Strip only content that is clearly irrelevant app chrome (see "Ignore" below).

TEXT_BLOCKS rules:
- Emit one entry per visible section, paragraph, chat message, log entry, listing row cluster, or form question. Do not collapse two sections into one block. Do not drop a section to keep the packet short.
- There is no upper limit. A document with six visible sections must produce six blocks. A chat with nine visible messages must produce nine blocks. A form with four answered questions must produce four blocks.
- If the block has a short visible header, field label, section title, chat sender, or question directly above or beside its body, put that header verbatim on its own first line of the block, then the body starting on the next line. Example: a Google Doc section titled `other information` followed by a paragraph becomes one TEXT_BLOCK whose first line is `other information` and whose remaining lines are that paragraph verbatim.
- Copy the body verbatim. Keep intra-block line breaks where the source visibly breaks lines, except when the line break is purely a soft-wrap inside a single paragraph (in which case join with a single space, preserving every word). Do not insert ellipses or abbreviations.
- If a block is only partially visible (cropped at the edge, covered by a tooltip, or clipped by a scroll region), include the visible portion exactly and describe the clipping in LIMITS.
- Each block must be self-contained: a later step reading only that block should know which section of the source it came from.

EXACT_TEXT rules:
- Use for short standalone strings whose exact wording matters but that are not the body of a TEXT_BLOCK: the page or listing title, a section heading that has no paragraph under it, single-value listing rows like `Used - like new`, `$50`, `Listed a week ago in San Francisco, CA`, one-line form answers, command-line invocations, error codes, hotkey strings, NEEDS_REVIEW markers, decisive buttons or statuses.
- Include every short reusable span that is visible and is not already contained verbatim inside a TEXT_BLOCK. There is no upper limit.
- Do not duplicate a span that already appears inside a TEXT_BLOCK.

Ignore (these are chrome, not content):
- Browser chrome, tab bars, address bars and URLs (unless the URL itself is the thing to paste), window controls, OS menubar, dock, notification chips, time/battery/wifi indicators.
- App chrome: toolbars, formatting ribbons, rulers, zoom indicators, page counters, character counts, version-history chips, editor cursors, selection handles.
- Navigation chrome: sidebars, workspace switchers, file trees, document outline panes, tab strips, breadcrumbs, unrelated navigation labels.
- Content that is visually outside the main document/chat/listing being shown and is not relevant to a paste.

But do NOT treat the following as chrome, even when styled like labels:
- Document section headings (bold or otherwise) that introduce body text.
- Form question prompts, field labels, helper text, and the visible answer beneath them.
- Chat senders, timestamps when they disambiguate the message, and the message body.
- Listing attribute labels that appear next to their values (`Condition`, `Location`, `Price`).
- Command prompts and their outputs in a terminal log.

Never invent hidden or cropped content. If text is cut off, include the visible portion and note the cropping in LIMITS. If a character is illegible, transcribe your best reading and mark uncertainty in LIMITS rather than guessing.

LAYOUT_HINTS:
- Very short notes only. Describe structure, not content. Examples: `Four stacked sections with bold headers each followed by one paragraph.`, `Chat transcript; the latest assistant message is the long paragraph at the bottom.`, `Two-column listing: left column holds attribute rows, right column holds the description.`. Use this to help a later step pick the right block. Do not re-summarize text that is already in TEXT_BLOCKS.

COMPLETENESS describes the source view, not the packet:
- Use `sufficient` only if the source screenshot itself shows a complete view of the relevant content - nothing important is cropped at the edges, no scroll bar suggests more content above or below, no tooltip / dropdown / overlay is hiding content the user might want to paste.
- Use `needs_source_image` if the source view is cut off, scrolled mid-document, partially clipped, hidden behind an overlay, or otherwise only a slice of a longer source the user could reasonably want content from. Examples: a long Google Doc scrolled mid-section, a chat with messages above the visible top, a marketplace listing whose details extend below the fold, a terminal whose earlier output has scrolled out of view, a long PDF page that is clipped.
- Treat this as a question about the source image's coverage, not about your transcription quality. Even if you transcribed every visible word into TEXT_BLOCKS perfectly, set `needs_source_image` when the visible view is itself incomplete.
